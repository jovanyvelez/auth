# Dependencias para DESPLEGAR en Vercel (la nube instala solo esto).
#
# - En local usamos uv ("uv sync"), que lee pyproject.toml y uv.lock.
# - En Vercel NO hace falta uvicorn ni fastapi[standard]: la nube ya
#   trae su propio servidor ASGI para ejecutar la función.
# - Versiones fijadas: en la nube corre exactamente lo que probaste.
fastapi==0.141.1
jinja2==3.1.6